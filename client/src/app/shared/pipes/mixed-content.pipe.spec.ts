import { MixedContentPipe } from './mixed-content.pipe';

describe('MixedContentPipe', () => {
  it('create an instance', () => {
    const pipe = new MixedContentPipe();
    expect(pipe).toBeTruthy();
  });
});
